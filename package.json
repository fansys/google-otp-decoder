{
  "name": "google-otp-decoder",
  "private": true,
  "version": "1.0.0",
  "description": "Local-only Google Authenticator migration decoder for Cloudflare Workers and Pages.",
  "scripts": {
    "deploy:pages": "wrangler pages deploy .",
    "deploy:worker": "wrangler deploy",
    "dev:pages": "wrangler pages dev .",
    "dev:worker": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.66.0"
  }
}

